server:
  port: 8009

spring:
  application:
    name: group-service
  config:
    import: optional:configserver:http://localhost:8001

# Service-specific details
info:
  app:
    name: SkillSync Group Service
    version: 1.0.0
