server:
  port: 8006

spring:
  application:
    name: mentor-service
  profiles:
    default: dev
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8001}

# Service-specific details
info:
  app:
    name: SkillSync Mentor Service
    version: 1.0.0
