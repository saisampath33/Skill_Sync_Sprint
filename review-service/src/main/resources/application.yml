server:
  port: 8010

spring:
  application:
    name: review-service
  profiles:
    default: dev
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8001}

# Service-specific details
info:
  app:
    name: SkillSync Review Service
    version: 1.0.0
