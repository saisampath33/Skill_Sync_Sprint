server:
  port: 8005

spring:
  application:
    name: user-service
  config:
    import: optional:configserver:http://localhost:8001

# Service-specific details
info:
  app:
    name: SkillSync User Service
    version: 1.0.0
