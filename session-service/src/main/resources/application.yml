server:
  port: 8008

spring:
  application:
    name: session-service
  config:
    import: optional:configserver:http://localhost:8001

# Service-specific details
info:
  app:
    name: SkillSync Session Service
    version: 1.0.0
